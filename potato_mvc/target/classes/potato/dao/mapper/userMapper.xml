<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="potato.userMapper">
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="uiVO">
		insert into member(id, pass, img, name, nick, birth, email, phone)
		values (#{ id }, #{ pass }, #{ img }, #{ name }, #{ nick }, #{ birth }, #{ email }, #{ phone })
	</insert>
	
	<!-- 로그인 -->
	<select id="selectMemberLogin" parameterType="lVO" resultType="ud">
		select id, nick
		from member
		where id= #{ id } and pass= #{ pass }
	</select>
	
	<!-- 로그인 -->
	<select id="selectManagerLogin" parameterType="mlVO"
	resultType="String">
		select manager_id
		from manager
		where manager_id= #{ manager_id } and pass= #{ pass }
	</select>
	
	<!-- 아이디 찾기 -->
	<select id="selectForgotId" parameterType="fiVO" resultType="String">
		select id
		from member
		where name= #{ name } and birth= #{ birth } and phone= #{ phone }
	</select>
	
	<!-- 비밀번호 찾기 -->
	<select id="selectForgotPw" parameterType="fpVO"
	resultType="String">
		select pass
		from member
		where id= #{ id } and name= #{ name } and phone= #{ phone }
	</select>
	
	<!-- 임시비밀번호 업데이트 -->
	<update id="updatePw" parameterType="fpVO">
		update member
		set pass = #{ temp }
		where id= #{ id } and name= #{ name } and phone= #{ phone }
	</update>
	
	<!-- 아이디 중복 확인 -->
	<select id="selectDuplChkId" parameterType="String" resultType="String">
		select id
		from member
		where id=#{ id }
	</select>
	
	<!-- 닉네임 중복 확인 -->
	<select id="selectDuplChkNick" parameterType="String" resultType="String">
		select nick
		from member
		where nick= #{ nick }
	</select>
	
</mapper>